# The OAuth Web Client ID from Google Cloud Console (dev project). Used by both
# the backend (verifies token audience) and the frontend (sign-in button).
GOOGLE_CLIENT_ID=

# Secret that signs our app session tokens. Use a long random value, e.g.
#   openssl rand -hex 32
APP_JWT_SECRET=
