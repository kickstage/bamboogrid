apiVersion: v1
kind: Service
metadata:
  name: {{ include "bamboogrid.fullname" . }}
  labels:
    {{- include "bamboogrid.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bamboogrid.selectorLabels" . | nindent 4 }}
